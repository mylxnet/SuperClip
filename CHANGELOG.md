# SuperClip 更新日志

## v2.0.0（2024-08） — 大版本定型

经过多轮迭代、需求调整、bug 修复后定型。本版本是「生产可用」基线，**仅修 bug 才会出 2.0.x 补丁**。

### 核心规格
- 目标框架：**.NET Core 3.1**（最后一个官方支持 Windows 7 的 .NET）
- UI 框架：**WPF**（XAML + MVVM 模式）
- 分发形态：**单文件自包含 exe**（约 130 MB），目标机无需装 .NET
- 支持系统：**Windows 7 SP1 / 8 / 8.1 / 10 / 11（x64）**
- 网络依赖：**完全离线**（可断网运行、可用防火墙验证）

### 功能
| 功能 | 描述 |
|---|---|
| 剪贴板历史 | 自动监听系统剪贴板，SHA-256 去重，>500 条自动清理（收藏不受影响），JSON 持久化 |
| Excel 表格拆分 | 含 `\t` 的 TSV 按「行→列」拆成独立单元格，标注「来自表格：第 X 行 第 Y 列」 |
| 复制模式开关 | 右键切换「一般复制（多行纯文本不拆）/ 表格复制（单列也拆）」 |
| 搜索 | 300ms 防抖实时过滤（内容 + 来源标注） |
| 类型筛选 | 全部 / 文本 / 表格 / 收藏 |
| 收藏 | ★/☆ 切换，置顶分组，永久保留，不参与清理 |
| 普通粘贴模式 | 双击条目 → 写剪贴板 + 模拟 Ctrl+V，条目变灰、位置不变 |
| 快速粘贴模式 | 单击选中，空格键粘贴，粘贴后沉底变灰，顶部自动选中下一条（连续空格连贴） |
| 复位 | 快速模式下恢复原始时间顺序并清除所有灰显 |
| 进程绑定 | 工具栏靶心图标（红=未绑定/绿=已绑定），点击进入点选模式（十字光标 + 全局鼠标钩子） |
| 窗口位置 | 默认贴屏幕右侧、宽 380、高 600 |
| 默认悬浮置顶 | 📌 按钮高亮，标题栏可关闭 |
| 单实例 | 全局命名 Mutex 防重复打开 |
| 托盘常驻 | 收起后继续监听剪贴板；双击托盘恢复，右键菜单「打开/退出」 |
| 全局热键 | `Ctrl + \`` 呼出/隐藏窗口 |
| 右键菜单 | 模式 / 复制模式 / 使用帮助 / 关于 |
| 新手帮助 | 9 步引导窗口 |
| 状态栏 | 左侧显示绑定进程名，右侧「Mr lin」署名 |
| exe 元数据 | 右键 → 属性 → 详细信息显示产品名、版本、版权、描述 |

### 工程化
| 工具 | 用途 |
|---|---|
| `build.bat` | 一键发布单文件 exe |
| `CleanAndBuild.bat` | 清缓存再发布（推荐每次发布前跑） |
| `PackageRelease.bat` | 发布 + 打包成 ZIP 便携安装包（含 install.bat / uninstall.bat） |
| `installer/install.bat` | 装到 `%ProgramFiles%\SuperClip\` + 桌面/开始菜单快捷方式（需管理员） |
| `installer/uninstall.bat` | 逆向清理（需管理员） |
| `IsExternalInit.cs` | 兼容 polyfill（.NET 5+ 框架自带，3.1 引用程序集缺失，需手动补） |

### 修复的关键 bug
- `CS0518`：`IsExternalInit` polyfill 解决 .NET Core 3.1 init 访问器编译失败
- `CS0407`：`Dispatcher.BeginInvoke` 接受 async lambda 的正确写法
- `CultureNotFoundException`：移除 `InvariantGlobalization` 解决 WPF 中文/日文 IME 启动崩溃
- UI 线程阻塞：剪贴板重试、粘贴等待改用 `await Task.Delay` 释放 UI
- history.json 损坏：`File.Replace` 原子写
- 列表选中/滚动丢失：`SyncInPlace` 原地增删而非整体重置
- 死代码：移除 `BtnMode_Click`、`ModeConverter`、`Deactivated` 处理器

### 已知约束
- exe 体积 130 MB（.NET 3.1 自包含固定开销；WPF 不支持 IL 剪裁）
- .NET Core 3.1 已于 2022-12 EOL；本程序纯本地无网络，安全性风险低
- 首次发布需联网（SDK 自动拉取 .NET 3.1 运行时包）

---

## 历史版本（已废弃，仅作记录）

| 版本 | 关键变化 |
|---|---|
| 1.0 | 初次可用版本 |
| 1.1 | 新增帮助窗口、状态栏、进程绑定、右键菜单、复制模式开关；多个 bug 修复 |
| 1.1.1 | 移除 PDB 发布、版本号精简到 csproj |
| 1.1.2 | UI 线程修复、原子写、增量更新、IME 修复 |
| **2.0.0** | **定型大版本**（本版本） |
