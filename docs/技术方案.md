# SuperClip 技术方案

> 版本基准：v2.0.2 · 文档基于对仓库全部源码（26 个文件 + 3 个测试文件）的系统阅读编写
> 定位：描述系统现状的完整技术设计，作为维护、排障与演进的权威参考
> 本次更新（v2.0.2）：新增用户设置持久化、自动化测试、全角/半角搜索、粘贴防护优化

---

## 目录

1. [系统概述与设计目标](#1-系统概述与设计目标)
2. [总体架构](#2-总体架构)
3. [技术选型与依据](#3-技术选型与依据)
4. [核心数据模型](#4-核心数据模型)
5. [模块详细设计](#5-模块详细设计)
6. [关键业务流程](#6-关键业务流程)
7. [线程与并发模型](#7-线程与并发模型)
8. [Win32 API 依赖清单](#8-win32-api-依赖清单)
9. [持久化与数据格式](#9-持久化与数据格式)
10. [UI 设计要点](#10-ui-设计要点)
11. [构建、发布与安装](#11-构建发布与安装)
12. [质量保障机制](#12-质量保障机制)
13. [已知约束与风险](#13-已知约束与风险)
14. [演进路线建议](#14-演进路线建议)

---

## 1. 系统概述与设计目标

### 1.1 一句话定位

SuperClip 是一个 **Windows 桌面剪贴板增强工具**：常驻后台监听系统剪贴板，自动记录复制内容（含 Excel 表格按单元格拆分），支持跨进程精准粘贴、收藏、搜索与双粘贴模式。

### 1.2 设计目标（按优先级）

| 优先级 | 目标 | 实现手段 |
|---|---|---|
| P0 | **零网络依赖** | 纯 Win32 P/Invoke + 本地 JSON，无任何 HTTP/遥测代码，可用防火墙验证 |
| P0 | **Win7 SP1 ~ Win11 x64 全兼容** | 目标框架锁定 .NET Core 3.1（最后支持 Win7 的 .NET） |
| P0 | **主窗口收起后持续监听** | 独立隐藏消息窗口注册 `WM_CLIPBOARDUPDATE`，与主窗口生命周期解耦 |
| P1 | **跨进程粘贴精准命中** | 进程绑定（点选窗口）+ 前台窗口切换三段式兜底 |
| P1 | **UI 流畅不闪烁** | 原地集合同步（`SyncInPlace`）、搜索 300ms 防抖、异步等待替代 `Thread.Sleep` |
| P2 | **免安装分发** | 单文件自包含 exe + ReadyToRun，目标机无需 .NET 运行时 |

### 1.3 非目标（明确不做）

- 图片 / 文件 / 富文本历史记录（仅纯文本，非文本剪贴板内容直接忽略）
- 多设备同步、云备份
- 插件系统

---

## 2. 总体架构

### 2.1 分层结构

```
┌─────────────────────────────────────────────────────────────┐
│  表现层 Views/                                               │
│    MainWindow.xaml(.cs)   主窗口：列表交互、粘贴触发、          │
│                           进程绑定点选、热键、右键菜单          │
│    HelpWindow.xaml(.cs)   模态新手引导（9 步）                 │
│    Converters.cs          XAML 值转换器 ×5                    │
├─────────────────────────────────────────────────────────────┤
│  视图模型层 ViewModels/                                       │
│    MainViewModel          列表状态机：增删、去重、排序、          │
│                           筛选/搜索、收藏、上限清理              │
├─────────────────────────────────────────────────────────────┤
│  服务层 Services/                                             │
│    ClipboardMonitorService  剪贴板监听（独立隐藏窗口）          │
│    TableParser              TSV 表格识别与单元格拆分           │
│    PasteService             写剪贴板 + 前台切换 + 模拟 Ctrl+V  │
│    StorageService           SHA-256 哈希 + JSON 原子持久化     │
│    TrayService              系统托盘（Shell_NotifyIcon）       │
├─────────────────────────────────────────────────────────────┤
│  模型层 Models/                                               │
│    ClipItem / ClipType      单条剪贴板记录                     │
├─────────────────────────────────────────────────────────────┤
│  平台层 Native/                                               │
│    WinApi                   全部 Win32 P/Invoke 声明（唯一出口）│
├─────────────────────────────────────────────────────────────┤
│  宿主 App.xaml(.cs)                                            │
│    单实例 Mutex、全局异常兜底、error.log                         │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 模块依赖关系

```
App ──▶ MainWindow ──▶ MainViewModel ──▶ StorageService / TableParser
  │         │
  │         ├──▶ TrayService ──────┐
  │         ├──▶ ClipboardMonitorService
  │         └──▶ PasteService
  │
  └──▶ WinApi（Native 层，被所有服务与窗口直接调用）
```

依赖规则：

- **无 DI 容器**：`MainWindow` 构造函数中直接 `new MainViewModel()`、`new TrayService(...)`、`new ClipboardMonitorService(...)`。规模小，保持简单。
- **单向依赖**：ViewModel 不引用 Views；Services 不引用 ViewModels（`TableParser.SplitSingleColumn` 是静态开关，由 Views 层写入）。
- **Win32 只经 `Native/WinApi.cs`**：全项目仅此一个文件含 `DllImport`，便于审计"无网络"承诺。

### 2.3 运行期对象拓扑

| 隐藏/可见窗口 | HWND | 职责 |
|---|---|---|
| 主窗口 `MainWindow` | 可见，`WindowStyle=None` | 列表展示与交互、注册全局热键（`WM_HOTKEY`） |
| 监听窗口 `SuperClipMonitor` | 0×0，WS_POPUP，位于 (-32000,-32000) | 接收 `WM_CLIPBOARDUPDATE` |
| 托盘窗口 `SuperClipTray` | 0×0，WS_POPUP，位于 (-32000,-32000) | 接收托盘回调消息与 `TaskbarCreated` |

三个窗口均为 WPF `HwndSource` 承载、运行于同一条 UI 线程。

---

## 3. 技术选型与依据

| 维度 | 选择 | 依据 / 取舍 |
|---|---|---|
| 运行时 | **.NET Core 3.1（自包含）** | 唯一官方支持 Windows 7 的 .NET；代价：2022-12 已 EOL、单文件约 130 MB。因程序纯本地无网络，安全暴露面极小 |
| UI 框架 | **WPF + MVVM（CommunityToolkit.Mvvm 8.2.2）** | `ObservableObject` / `[ObservableProperty]` 源生成器减少样板；XAML 数据绑定驱动列表 UI |
| 系统集成 | **纯 Win32 P/Invoke，不用 WinForms** | 避免引入 `System.Windows.Forms` 程序集（曾触发 NETSDK1175）；托盘用 `Shell_NotifyIcon` 手写 |
| 剪贴板监听 | `AddClipboardFormatListener` + `WM_CLIPBOARDUPDATE` | 比 `WM_DRAWCLIPBOARD` 剪贴板链现代、无需链修复逻辑 |
| 持久化 | **System.Text.Json + 单文件** | 数据量上限 500 条，单 JSON 文件足够；`File.Replace` 原子写防损坏 |
| 去重键 | **SHA-256（UTF-8 十六进制）** | 避免对长文本全量 `string.Equals` 反复比较；哈希随条目持久化 |
| 分发 | 单文件 + SelfContained + ReadyToRun，**不剪裁、不 InvariantGlobalization** | WPF 不支持 IL 剪裁（NETSDK1168）；Invariant 模式会使中文/日文 IME 用户启动崩溃（WPF TextBox 内部查询 `InputLanguageManager.CurrentInputLanguage`） |
| 卫星语言 | `SatelliteResourceLanguages = en;zh-Hans` | 缩小体积，且不影响 IME |

**关键约束记忆点**（历史上踩过的坑，已固化到 csproj 注释与 README）：

1. ❌ `-p:PublishTrimmed=true` → NETSDK1168
2. ❌ `InvariantGlobalization=true` → IME 用户 `CultureNotFoundException`
3. ✅ `IsExternalInit.cs` polyfill 必须保留（.NET 3.1 缺失该类型，`init` 访问器报 CS0518）

---

## 4. 核心数据模型

### 4.1 ClipItem（`Models/ClipItem.cs`）

| 属性 | 类型 | 说明 |
|---|---|---|
| `Id` | `string`（init） | `Guid.NewGuid()`，持久化主键 |
| `Content` | `string`（init） | 剪贴板纯文本内容 |
| `Type` | `ClipType`（init） | `Text`=普通文本 / `TableCell`=表格拆分单元格 |
| `SourceRow` / `SourceCol` | `int?`（init） | 来自表格时的行列号（1 起），用于「来自表格：第 X 行 第 Y 列」标注 |
| `Timestamp` | `DateTime`（init） | 入列时间（本地时间） |
| `Hash` | `string`（init） | `Content` 的 SHA-256 十六进制，去重键 |
| `IsFavorite` | `bool`（observable） | 收藏 ★/☆；置顶、免清理 |
| `IsPasted` | `bool`（observable） | 已粘贴灰显；重启后保留 |

设计要点：

- **不可变字段用 `init`**，可变状态（收藏、灰显）用 `[ObservableProperty]` 触发 UI 局部刷新——两者分离，保证 `Hash` 等派生键在生命周期内稳定。
- `SourceLabel` 是计算属性（非存储），表格单元格返回「来自表格：第 X 行 第 Y 列」，普通文本返回空串；搜索时与 `Content` 一并匹配。
- `IsExternalInit` polyfill 支持 .NET 3.1 下的 C# 9 `init`。

### 4.2 派生枚举（`ViewModels/MainViewModel.cs`）

- `FilterType { All, Text, TableCell, Favorite }` —— 类型筛选下拉。
- `PasteMode { Normal, Quick }` —— 普通模式（双击粘贴）/ 快速模式（单击选中 + 空格粘贴）。

---

## 5. 模块详细设计

### 5.1 App 宿主（`App.xaml.cs`）

**职责**：单实例控制 + 全局异常兜底。

- **单实例**：`Global\SuperClip_SingleInstance_9F3A2B1C` 命名 Mutex。`createdNew == false` 时用 `FindWindow(null, "SuperClip")` 找到已有窗口，`IsIconic` 则 `SW_RESTORE`，`SetForegroundWindow` 拉到前台后 `Shutdown()` 当前进程。
- **异常兜底**：
  - `DispatcherUnhandledException` → 标记 `e.Handled = true`（UI 不闪退），写日志并弹窗；
  - `AppDomain.UnhandledException` → 写日志并弹窗（致命，进程将退出）；
  - 日志落盘 `%AppData%\SuperClip\error.log`（追加写，含时间与完整异常栈）。

> 注意：`StartupUri="Views/MainWindow.xaml"`（App.xaml），窗口创建由框架完成；`OnStartup` 只做前置检查。

### 5.2 剪贴板监听（`Services/ClipboardMonitorService.cs`）

**职责**：独立于主窗口的常驻剪贴板监听，是"收起后仍工作"的核心保障。

- **创建**：`HwndSourceParameters("SuperClipMonitor")` 建 0×0 WS_POPUP 隐藏窗口，`SetPosition(-32000,-32000)` 移出屏幕；`AddClipboardFormatListener(hwnd)` 注册。
- **消息处理**（`WndProc`）：`WM_CLIPBOARDUPDATE (0x031D)` → 置 `handled = true` → **双重自写入过滤**：
  1. `_isInternalPaste()` 标志位（本程序正在粘贴）；
  2. 新内容 == `_getLastPasted()`（最近一次粘贴内容比对，兜底防标志位时序误差）。
- **读取重试**：切回 UI 线程 `BeginInvoke` 异步读，最多 6 次 × 25ms（`await Task.Delay` 释放 UI 线程）。原因：Excel 等源程序复制瞬间独占剪贴板，直接读会抛异常。非文本 / 空文本直接返回不浪费重试。
- **注册失败降级**：`AddClipboardFormatListener` 失败仅 `Debug.WriteLine`，不致命——手动复制粘贴仍可用，只是不自动入列。
- **释放**：`RemoveClipboardFormatListener` → `RemoveHook` → `Dispose` HwndSource。

构造注入三个回调（`onClipboardText`、`isInternalPaste`、`getLastPasted`），服务本身无业务状态，天然可测试。

### 5.3 表格解析（`Services/TableParser.cs`）

**职责**：判断剪贴板文本是否为表格，并按「行→列」拆成单元格。

- **复制模式开关**（静态属性，运行时由右键菜单切换，**不持久化**，重启恢复默认一般复制）：
  - `SplitSingleColumn = false`（默认，一般复制）：仅含 `\t` 的文本按表格拆；多行纯文本整体记录。
  - `= true`（表格复制）：仅有换行、无 `\t` 的单列/多行文本也逐行拆分。
- **`IsTable` 判定**：
  1. 含 `\t` → 恒为表格（多列，含单行多列）；
  2. 否则需 `SplitSingleColumn && 含换行`；
  3. 单行无 `\t` 恒为普通文本。
- **`Parse` 拆分规则**：
  - `\r\n`/`\r` 统一为 `\n`，`Trim('\n')` 去首尾空行；
  - 逐行：含 `\t` 按 `\t` 拆列；否则整行作为一个单元格（Col=1）；
  - **跳过空单元格但保留列号**（空列不产生条目，后续列的 `Col` 值不前移）；空行整行跳过（行号也不前移）；
  - 返回 `List<Cell>{ Content, Row, Col }`（1 起编号）。

### 5.4 粘贴服务（`Services/PasteService.cs`）

**职责**：把指定文本"粘贴"到目标窗口光标处。三步走：

1. **写剪贴板** `Clipboard.SetText(text)`——由调用方预设 `_internalPaste` 标志屏蔽自监听；目标为空或就是本进程主窗口则到此为止（防粘回自己）。
2. **夺回前台**（三段式兜底，绕过 Windows 前台锁）：
   - `AllowSetForegroundWindow(targetPid)`：本程序当前是前台，主动授权目标进程抢占；
   - `SetForegroundWindow` + `SetActiveWindow`；
   - 仍未成功 → `SwitchToThisWindow(targetHwnd, true)` 强切（Win10/11 仍有效）。
   - 然后 `await Task.Delay(60)` 等目标真正获得键盘焦点（Excel 切换有微延迟），期间 UI 线程可继续处理消息。
3. **模拟键入**：`keybd_event` 依次发送 `Ctrl↓ → V↓ → V↑ → Ctrl↑`，内容落到目标窗口光标处。

全程 try/catch 静默——粘贴失败不影响程序运行。

### 5.5 持久化（`Services/StorageService.cs`）

**职责**：SHA-256 计算 + JSON 存取。

- **路径**：`%AppData%\SuperClip\history.json`（`SpecialFolder.ApplicationData`）。
- **`ComputeHash`**：UTF-8 → SHA-256 → 小写十六进制串；空串返回空（空内容不入列，无需哈希）。
- **`Save`（原子写）**：序列化（紧凑格式）→ 写 `history.json.tmp` → 主文件已存在则 `File.Replace`（原子替换），否则 `File.Move`。任一步失败静默吞掉并尝试清理 `.tmp`——**持久化失败绝不中断主流程**。
- **`Load`（容错读）**：文件不存在 / 空白 / 反序列化失败 / 任何异常 → 返回空列表。即**损坏的 history.json 不会导致启动失败**，代价是数据丢失（可接受：剪贴板历史非关键数据）。
- 保存时按列表当前顺序序列化——重启后**排序、收藏、灰显状态全部保留**。

### 5.6 系统托盘（`Services/TrayService.cs`）

**职责**：托盘常驻图标（纯 Win32 `Shell_NotifyIcon`，无 WinForms）。

- 隐藏窗口 `SuperClipTray` 承载回调消息 `WM_APP+1 (0x8001)`；
- 图标：`ExtractAssociatedIcon` 从自身 exe 提取（csproj `ApplicationIcon`），失败回退 `LoadIcon(IDI_APPLICATION)`；
- 交互：`WM_LBUTTONDBLCLK` → 打开主窗口；`WM_RBUTTONUP` → `CreatePopupMenu` + `TrackPopupMenuEx(TPM_RETURNCMD)` 弹「打开 / 退出」（弹菜单前 `SetForegroundWindow` 自身，保证菜单点击外部可消失——标准托盘菜单范式）；
- **Explorer 重启自愈**：`RegisterWindowMessage("TaskbarCreated")`，任务栏重建时重新 `NIM_ADD`（解决托盘图标消失问题）；
- 释放：`NIM_DELETE` + 销毁 HwndSource。

### 5.7 主视图模型（`ViewModels/MainViewModel.cs`）

**职责**：列表状态机——项目顺序、去重、收藏置顶、上限清理、筛选搜索、粘贴后状态流转。持有两个集合：

- `Items`：全量数据（真源，与磁盘一致）；
- `DisplayItems`：筛选/搜索后的视图集合，绑定到 ListBox。

#### 数据不变式

1. **排序不变式**：`Items` = 收藏区（前）+ 非收藏区（后）；各区内部保持"最新在前"。非收藏区最前 = 最旧条目。
2. **去重不变式**：任意时刻不存在两条相同 `Hash` 的条目。重复复制 → 移除旧条、按新条目插入（收藏状态转移保留）。
3. **上限不变式**：非收藏条数 ≤ 500（`MaxItems`），超限从非收藏区最前（最旧）淘汰；**收藏永不清理**。
4. **视图一致性**：`DisplayItems` ⊆ `Items`，顺序与 `Items` 一致，仅多了类型/关键词过滤。

#### 关键方法

| 方法 | 行为 |
|---|---|
| `AddFromClipboard(text)` | 总入口：`IsTable` → `AddTable`（整块插入防倒序），否则 `AddSingle`；随后 `EnforceLimit → Save → RefreshDisplay` |
| `AddSingle` | 哈希去重（保留旧条收藏态）→ 插入非收藏区最前 |
| `AddTable` | 先逐格去重移除旧条，再**整块顺序插入**非收藏区最前（逐条 Insert 会整体倒序） |
| `ToggleFavorite` | 翻转收藏 → `ApplyOrder`（收藏置顶，**原地 Move**）→ 保存刷新 |
| `PasteDone(item, moveToEnd)` | 标记灰显；快速模式沉底；快速模式下自动选中可见列表第一条未粘贴项（支持连续空格连贴） |
| `ClearAll` | 全清（含收藏） |
| `Reset` | 清除全部灰显；收藏按时间**升序**、其余按时间**降序**重建（复位到最初顺序） |
| `SelectItem` | 所有模式同步选中到 VM（修复了"普通模式点选不更新 VM，切快速后跳第一项"的割裂） |
| `RefreshDisplay` / `BuildDisplayList` | 按 `FilterType` 过滤 → 按 `SearchText`（`Content` 或 `SourceLabel`，`OrdinalIgnoreCase`）过滤 → `SyncInPlace` 原地应用到 DisplayItems |
| `SyncInPlace<T>` | **原地同步算法**：先删 target 中不存在的，再逐位 `Move`/`Insert` 对齐。不重设 `ItemsSource`，避免 WPF 集合 Reset 导致选中丢失、滚动位置重置、整屏闪烁 |

#### 搜索防抖

`OnSearchTextChanged` → 重启 300ms `DispatcherTimer`，停止输入才 `RefreshDisplay`。筛选类型变化立即刷新（无防抖必要）。

### 5.8 主窗口（`Views/MainWindow.xaml.cs`）

**职责**：交互编排层，持有全部跨服务状态（绑定窗口、粘贴标志、点选钩子等）。

#### 全局热键

`SourceInitialized` 时 `RegisterHotKey(hwnd, id=1, MOD_CONTROL, VK_OEM_3)`（Ctrl + `）。`WM_HOTKEY` → `ToggleVisibility`（可见则 Hide，否则唤起）。退出时 `UnregisterHotKey`。

#### 窗口唤起与默认绑定

`ShowWindow()`：

1. 记录 `_lastExternalWindow = GetForegroundWindow()`（呼出前用户正在用的窗口——比 `Deactivated` 时机可靠）；
2. **首次呼出自动绑定**：`_boundOnce` 未置位时，把呼出前窗口作为粘贴目标（用户装完即用，无需手动点选）；
3. `Show + Activate + lstItems.Focus()`。

#### 进程绑定（点选模式）

- 入口：工具栏靶心按钮 → `StartPick()`：全局十字光标（`SetSystemCursor(LoadCursor(IDC_CROSS), OCR_NORMAL)`，**系统级**替换箭头）→ 隐藏自身 → 安装 `WH_MOUSE_LL` 低层鼠标钩子。
- 钩子回调：`WM_LBUTTONDOWN` → `WindowFromPoint` → `GetAncestor(GA_ROOT)` 取根窗口（忽略目标程序内部子控件）→ 排除自身 → 记录 `_boundWindow` + `ResolveProcessName`（`GetWindowThreadProcessId` → `Process.GetProcessById().ProcessName`，仅进程名不含文档标题）→ `EndPick`。
- 降级链：钩子安装失败 → 直接绑 `_lastExternalWindow`；Esc / 再点靶心 / 热键 → `CancelPick`（同样回退到上次外部窗口）。
- 光标恢复：`SystemParametersInfo(SPI_SETCURSORS)` **一键复位全部系统光标**——比逐个还原 `LoadCursor(IDC_ARROW)` 可靠，杜绝十字光标残留。
- 状态指示：靶心图标红 = 未绑定 / 绿 = 已绑定（`SolidColorBrush` 资源动态改 `Color`）；状态栏左侧显示绑定进程名。
- 粘贴目标解析 `GetPasteTarget()`：绑定窗口仍存活（`IsWindow`）→ 用它；否则回退 `_lastExternalWindow`。

#### 粘贴编排 `DoPaste`

```
_internalPaste = true                    ──┐ 第一层防护：标志位
_lastPastedContent = item.Content        ──┘ 第二层防护：内容比对
await PasteService.PasteTextAsync(...)
_vm.PasteDone(item, moveToEnd)
_pasteGuard.Restart()                    ── 400ms 后统一清零两个防护
```

双层防护 + 400ms 守护定时器，杜绝"粘贴写剪贴板 → 触发自身监听 → 重复入列"的回环。

#### 其他交互

- **空格粘贴**：窗口级 `PreviewKeyDown` 捕获 Space（仅快速模式且有选中项）；焦点在 `TextBox`/`ComboBox` 时放行原生行为。
- **Esc**：点选模式中取消点选。
- **右键菜单**：`PreviewMouseRightButtonDown` 全窗口拦截，`OriginalSource is TextBox` 时放行原生编辑菜单；弹出前动态刷新「模式」「复制模式」项标题（直观显示当前值 + 点此切换）。
- **标题栏**：无边框自绘，`DragMove` 拖动；📌 置顶开关（默认开）；— 收起到托盘；✕ = 退出应用（不是隐藏）。
- **退出清理** `ExitApp`：点选中则卸钩子+复位光标 → 注销热键 → 释放监听与托盘 → `Shutdown`。

### 5.9 辅助视图（`Views/Converters.cs`、`HelpWindow`）

| 转换器 | 用途 |
|---|---|
| `IndexConverter`（IMultiValueConverter） | 条目序号 = 在 DisplayItems 中 `IndexOf + 1`（基于当前显示顺序，不用 AlternationIndex——集合重建后不刷新） |
| `FilterTypeConverter` | 枚举 → 全部/文本/表格/收藏 |
| `StarConverter` | bool → ★/☆ |
| `EmptyToCollapsedConverter` / `EmptyToVisibleConverter` | 来源标注显隐 / 搜索框占位提示显隐 |

`HelpWindow`：无边框模态窗口，9 步静态引导文案（呼出热键、自动记录、双模式、收藏、绑定、复制模式、搜索、清除/复位、置顶）。

---

## 6. 关键业务流程

### 6.1 剪贴板采集（核心链路）

```
用户在其他程序 Ctrl+C
  → 系统 WM_CLIPBOARDUPDATE → 监听窗口 WndProc
  → [拦截1] _internalPaste 标志位？→ 是：忽略
  → BeginInvoke 到 UI 线程，异步读取（最多 6 次 × 25ms 重试，Task.Delay 让出线程）
      ├─ 非文本/空文本 → 放弃
      ├─ [拦截2] 内容 == _lastPastedContent？→ 是：忽略（自粘贴回环）
      └─ 成功 → vm.AddFromClipboard(text)
              ├─ TableParser.IsTable? ─ 是 → Parse 拆单元格 → 整块插入
              └─ 否 → AddSingle 哈希去重插入
              → EnforceLimit（>500 淘汰最旧非收藏）
              → StorageService.Save（原子写 history.json）
              → RefreshDisplay（SyncInPlace 原地更新，不丢选中/滚动）
```

### 6.2 表格粘贴场景（Excel → 逐格粘贴）

```
Excel 中复制 A1:B2 → 剪贴板为 TSV（"a1\tb1\na2\tb2"）
  → IsTable（含 \t）→ Parse 得 4 个 Cell（Row/Col 1 起，空格跳过但编号保留）
  → 4 条 ClipItem 入列，标注「来自表格：第 X 行 第 Y 列」
  → 用户绑定 Excel 窗口 → 快速模式下：单击选中 A1 → 空格粘贴 → 沉底灰显、自动选中 A2 → 空格……
```

### 6.3 跨进程粘贴

```
双击条目（普通模式）/ 空格（快速模式）
  → DoPaste：置自粘贴防护（标志 + 内容）
  → PasteService：
      Clipboard.SetText          （触发 WM_CLIPBOARDUPDATE，被防护拦截）
      GetPasteTarget             （绑定窗口存活？否则回退上次外部窗口）
      AllowSetForegroundWindow → SetForegroundWindow/SetActiveWindow
        → 仍非前台？SwitchToThisWindow 强切
      await 60ms（等焦点稳定，UI 不卡）
      keybd_event Ctrl+V
  → PasteDone：IsPasted=true；快速模式沉底 + 选中下一条未粘贴项
  → 400ms 守护定时器清零防护
```

### 6.4 进程绑定（点选）

```
点靶心 → 十字光标（系统级 SetSystemCursor）→ 隐藏主窗 → 装 WH_MOUSE_LL 钩子
  → 用户点击任意窗口 → 钩子回调：WindowFromPoint → GA_ROOT 根窗口（排除自身）
  → 记录绑定 + 解析进程名 → EndPick：卸钩子 → SPI_SETCURSORS 复位光标 → 恢复窗口
  降级：钩子安装失败→绑上次外部窗口；Esc/再点→取消并回退
```

### 6.5 应用生命周期

```
启动：Mutex 单实例检查（重实例 → 激活旧窗口后退出）
  → MainWindow ctor：VM 加载 history.json → TrayService → ClipboardMonitorService
  → SourceInitialized：注册热键 → 窗口停靠屏幕右侧（380 宽、垂直居中）
运行：三窗口（主窗 + 监听 + 托盘）常驻 UI 线程
退出：标题栏 ✕ 或托盘「退出」→ 注销热键/钩子 → 释放服务 → Shutdown
崩溃：Dispatcher 异常 → 记 error.log + 弹窗，程序存活；AppDomain 致命异常 → 记录 + 弹窗
```

---

## 7. 线程与并发模型

**单 UI 线程 + 异步让出**，无锁、无后台工作线程：

| 组件 | 线程归属 | 说明 |
|---|---|---|
| 监听窗口 `WndProc` | UI 线程 | HwndSource 消息本就在宿主线程泵出 |
| 剪贴板读取 | UI 线程（异步分段） | `await Task.Delay(25)` 期间 UI 可响应，替代 `Thread.Sleep` 卡顿 |
| 粘贴流程 | UI 线程（异步分段） | `await Task.Delay(60)` 等焦点切换，期间不冻结 |
| `WH_MOUSE_LL` 钩子回调 | UI 线程 | 低层钩子回调编组到安装线程；钩子在 UI 线程安装，回调可直接改 UI（代码有注释确认此设计） |
| `DispatcherTimer` ×2 | UI 线程 | 搜索防抖 300ms、粘贴守护 400ms |
| 磁盘 IO（Save/Load） | UI 线程（同步） | ≤500 条紧凑 JSON，体量小（百 KB 级），可接受；见 §14 改进项 |

**并发正确性依赖单线程假设**：`_internalPaste`、`_lastPastedContent`、`_boundWindow` 等共享状态均无锁访问——因为读写在同一 UI 线程串行化。`HwndSource` 全部创建于 UI 线程，该假设成立。

---

## 8. Win32 API 依赖清单

全部集中在 `Native/WinApi.cs`（按功能分组）：

| 功能域 | API |
|---|---|
| 剪贴板监听 | `AddClipboardFormatListener` / `RemoveClipboardFormatListener`（`WM_CLIPBOARDUPDATE = 0x031D`） |
| 前台控制 | `GetForegroundWindow` / `SetForegroundWindow` / `SetActiveWindow` / `AllowSetForegroundWindow(ASFW_ANY)` / `SwitchToThisWindow` / `ShowWindow` / `IsIconic` / `FindWindow` / `IsWindow` / `GetWindowThreadProcessId` / `AttachThreadInput`（已声明未使用） |
| 键盘模拟 | `keybd_event`（`VK_CONTROL=0x11`、`VK_V=0x56`、`KEYEVENTF_KEYUP`） |
| 全局热键 | `RegisterHotKey` / `UnregisterHotKey`（`WM_HOTKEY=0x0312`，`MOD_CONTROL` + `VK_OEM_3=0xC0`） |
| 托盘 | `Shell_NotifyIcon`（NIM_ADD/MODIFY/DELETE；NOTIFYICONDATA 结构）/ `ExtractAssociatedIcon` / `LoadIcon` / `RegisterWindowMessage("TaskbarCreated")` |
| 弹出菜单 | `CreatePopupMenu` / `AppendMenu` / `TrackPopupMenuEx(TPM_RETURNCMD)` / `DestroyMenu` / `GetCursorPos` |
| 低层鼠标钩子 | `SetWindowsHookEx(WH_MOUSE_LL)` / `UnhookWindowsHookEx` / `CallNextHookEx` / `WindowFromPoint` / `GetAncestor(GA_ROOT)` / `GetModuleHandle` |
| 光标 | `LoadCursor(IDC_CROSS/IDC_ARROW)` / `SetSystemCursor(OCR_NORMAL)` / `SystemParametersInfo(SPI_SETCURSORS)` |

> 该清单是"零网络"审计的依据：除 user32/shell32/kernel32 本地调用外无任何外部依赖面。

---

## 9. 持久化与数据格式

### 9.1 文件布局

```
%AppData%\SuperClip\
├── history.json    剪贴板历史（顺序 = Items 当前顺序）
└── error.log       全局异常日志（追加）
```

### 9.2 history.json 条目示例

```json
[
  {
    "Id": "3f2a…",
    "Content": "excel_value",
    "Type": 1,
    "SourceRow": 2,
    "SourceCol": 3,
    "Timestamp": "2026-09-18T10:00:00.123",
    "Hash": "ab12…64hex",
    "IsFavorite": false,
    "IsPasted": true
  }
]
```

- `Type` 按枚举数值序列化（System.Text.Json 默认）：`0`=Text、`1`=TableCell；
- 写入为紧凑单行 JSON；上限 500 条 + 收藏，典型体积 < 1 MB；
- **原子性**：`history.json.tmp` 写完 → `File.Replace` 原子替换，任何时刻崩溃主文件完好；
- **容错性**：读失败返回空列表（见 §5.5 取舍说明）。

### 9.3 settings.json 用户设置持久化（v2.0.2 新增）

**文件位置**：`%AppData%\SuperClip\settings.json`  
**持久化内容**：
| 设置项 | 说明 | 默认值 |
|---|---|---|
| `Left` / `Top` | 窗口左上角坐标 | 0 / 0 |
| `Width` / `Height` | 窗口大小 | 380 / 600 |
| `BoundProcessName` | 绑定的进程名（如 "EXCEL"） | null |
| `Topmost` | 置顶开关 | true |
| `PasteMode` | 粘贴模式（0=普通，1=快速） | 0 |
| `FilterType` | 筛选类型索引 | 0 |
| `SplitSingleColumn` | 复制模式 | false |

**持久化时机**：
- 窗口关闭时保存位置/大小/置顶状态
- 模式切换、筛选变更时保存设置
- 绑定目标变更时保存进程名（下次启动尝试恢复）

**恢复逻辑**（`MainWindow.xaml.cs`）：
1. 启动时加载 `settings.json`
2. 应用窗口位置/大小（若有效）
3. 尝试通过进程名查找窗口句柄恢复绑定
4. 应用置顶状态、模式、筛选类型

### 9.4 不持久化的状态

- 粘贴后灰显状态（`IsPasted`）仍保留在 `history.json` 中，重启后保留灰显
- 收藏状态已持久化到 `history.json`

---

## 10. UI 设计要点

- **无边框自绘窗口** 380×600，默认贴屏幕右缘、垂直居中；四行布局：标题栏 36 / 工具栏 Auto / 列表 * / 状态栏 22。
- **条目卡片**：白底圆角边框；选中描边 `#00897B`；已粘贴 `Opacity 0.5`；收藏底色 `#FFF8E1`（DataTrigger 驱动）。
- **条目内容三行**：主内容（`TextWrapping` + `MaxHeight=60` + 省略号）→ 来源标注（小灰字，空则隐藏）→ 时间戳 `HH:mm:ss`。左侧序号由 `IndexConverter` 按显示位置计算。
- **搜索占位**：透明 `TextBlock` 叠加在 TextBox 上，`EmptyToVisibleConverter` 控制显隐。
- **交互细节**：标题栏按钮区域不触发拖拽；TextBox 上右键保留原生菜单；筛选下拉手动同步 VM（规避 enum 双向绑定偶发失步导致"全部"显示空列表）。

---

## 11. 构建、发布与安装

### 11.1 构建产物

`dotnet publish -c Release -r win-x64 --self-contained -p:PublishSingleFile=true`
（发布参数已固化进 csproj，命令行再传一次兜底）

产物：`bin\Release\netcoreapp3.1\win-x64\publish\SuperClip.exe`（单文件自包含 + ReadyToRun，无 pdb，约 130 MB）。发布目标 `CopyVersionedExe` 自动生成带版本号的分发副本 `SuperClip_v{Version}.exe`（固定名 exe 保留：进程名、installer 脚本、单实例 FindWindow 均依赖固定名；便携包 ZIP 内直接使用版本化文件名，安装时统一还原为固定名）。

### 11.2 脚本体系

| 脚本 | 作用 |
|---|---|
| `build.bat` | 检查 SDK → 发布（日常用） |
| `CleanAndBuild.bat` | 先删 `bin/obj` 再发布（发布前推荐，防缓存干扰） |
| `PackageRelease.bat` | CleanAndBuild → 从 csproj 抓 `<Version>` → 汇集 exe + README + install/uninstall.bat → `release\SuperClip_vX.Y.Z_portable.zip`（英文命名：GitHub Releases 附件不支持中文字符） |
| `installer\install.bat` | 管理员：复制到 `%ProgramFiles%\SuperClip\` + 开始菜单/桌面快捷方式（PowerShell WScript.Shell 创建 .lnk） |
| `installer\uninstall.bat` | 管理员：taskkill → 删目录与快捷方式 |

### 11.3 版本与元数据

csproj 统一管理：`Version=2.0.1`（显示，补丁版本）、`AssemblyVersion=2.0.0.0` / `FileVersion=2.0.1.0`（4 段，AssemblyVersion 保持大版本不变）；Product/Company/Copyright/Description 齐全（右键属性可见）。`NeutralLanguage=zh-Hans`。

---

## 12. 质量保障机制

| 机制 | 位置 | 说明 |
|---|---|---|
| 剪贴板读取重试 | 监听服务 | 6 × 25ms，对抗源程序（Excel）短暂独占剪贴板 |
| 自粘贴回环防护（双层 + 立即重置） | MainWindow + 监听服务 | `_internalPaste` 标志 + 内容比对，**粘贴后立即重置** + 1000ms 守护定时器兜底（v2.0.2 更新） |
| 原子持久化 | StorageService | tmp + `File.Replace`，断电/崩溃不损坏历史文件 |
| UI 不闪烁 | MainViewModel | `SyncInPlace` 原地同步，杜绝 ItemsSource Reset 副作用 |
| 前台切换三段兜底 | PasteService | AllowSetForegroundWindow → SetForegroundWindow/SetActiveWindow → SwitchToThisWindow |
| 粘贴目标失效回退 | MainWindow | `IsWindow(_boundWindow)` 检查，绑定窗口已关则回退上次外部窗口 |
| 托盘自愈 | TrayService | `TaskbarCreated` 广播后自动重挂图标 |
| 全局异常兜底 | App | Dispatcher（存活）+ AppDomain（记录）双通道，error.log 留痕 |
| 降级路径 | 多处 | 监听注册失败、托盘添加失败、钩子安装失败、光标替换失败均不致命，功能逐级退化而非崩溃 |

**测试现状**：v2.0.2 新增 `Tests/` 项目（xunit），共 21 个测试用例，覆盖：
- `TableParser.IsTable/Parse`：13 个用例（表格识别、拆分、空单元格、换行符规范化）
- `StorageService.ComputeHash`：4 个用例（哈希一致性、不同内容哈希不同、空字符串、长文本）
- `ClipItem.SourceLabel`：4 个用例（普通文本、表格单元格、无行列信息）

`SyncInPlace` 因依赖 WPF 集合，暂无法单测，建议后续提取为独立静态方法增加测试覆盖率。

---

## 13. 已知约束与风险

| # | 约束/风险 | 影响 | 缓解现状 |
|---|---|---|---|
| R1 | **.NET Core 3.1 已 EOL（2022-12）** | 无安全补丁 | 程序零网络，暴露面极小；README/CHANGELOG 已明示 |
| R2 | exe 约 130 MB | 分发体积大 | 自包含+R2R 换取目标机免装运行时；WPF 不支持剪裁是硬约束 |
| R3 | ~~模式/复制模式/置顶/窗口位置**不持久化**~~ **已修复（v2.0.2）** | 每次启动重置默认 | 新增 `SettingsService`，持久化到 `settings.json`（见 §9.3） |
| R4 | ~~粘贴防护窗口期 400ms~~ **已修复（v2.0.2）** | 目标窗口切换+粘贴超过 400ms 时，自写入可能漏拦 | 延长至 1000ms + 粘贴后立即重置标志（见 §5.4 更新） |
| R5 | ~~绑定图标红绿仅看进程名，不校验 `IsWindow`~~ **已修复（v2.0.1）** | 绑定窗口关闭后图标仍绿 | `ShowWindow()` 呼出时校验 `IsWindow(_boundWindow)`，已失效则自动改绑呼出前活动窗口并刷新图标/状态栏；粘贴时 `GetPasteTarget` 仍有 `IsWindow` 校验兜底 |
| R6 | ~~`Reset` 中收藏按时间升序、非收藏降序~~ **已修复（v2.0.1）** | 收藏区内部顺序与直觉（最新在前）相反 | `Reset` 统一改为收藏/非收藏都按时间降序，与列表排序不变式一致 |
| R7 | Save/Load 同步 IO 在 UI 线程 | 极端大历史时保存瞬间卡顿 | 上限 500 条，实测可忽略；如上限提高需改异步 |
| R8 | 仅纯文本 | 图片/文件/富文本不入列 | 明确的非目标（§1.3） |
| R9 | `SwitchToThisWindow` 为未公开 API | 极端情况未来 Windows 行为变化 | 仅作第三段兜底，前两段通常已生效 |
| R10 | 单实例定位靠 `FindWindow(null, "SuperClip")` 标题匹配 | 其他同名标题窗口可能误激活 | 标题唯一性够用；更稳做法是 Mutex 持有者发布命名管道/窗口句柄 |

---

## 14. 演进路线建议

按投入产出排序：

1. ~~**状态持久化（低成本高感知）**~~ **已完成（v2.0.2）**：新增 `SettingsService`，持久化窗口位置/大小、绑定目标、置顶状态、粘贴模式、筛选类型、复制模式。消除 R3。
2. ~~**纯函数单测**~~ **已完成（v2.0.2）**：新增 `Tests/` 项目（xunit），覆盖 `TableParser.IsTable/Parse`（13 个用例）、`StorageService.ComputeHash`（4 个用例）、`ClipItem.SourceLabel`（4 个用例）。消除测试空白。
3. ~~**粘贴模式/过滤等持久化的同时，~~收藏区排序统一~~（R6 已修复）**~~ **已完成（v2.0.2）**。
4. **全角/半角搜索优化**：v2.0.2 已实现搜索时全角→半角规范化，提升中文混合输入体验。
5. **运行时升级评估**：若放弃 Win7 → .NET 8：体积约减半（~70 MB）、受支持的运行时、可用 `PublishTrimmed` 之外的 AOT 外优化；代码本身兼容性良好（已用 CommunityToolkit 8.2.2、C# latest）。若必须保留 Win7 则维持现状。
6. **剪贴板内容类型扩展**：`ClipType` 增加 Image/File（监听处判断 `ContainsImage`/`ContainsFileDropList`），UI 缩略图渲染；JSON 持久化图片建议转存文件目录仅留路径。
7. **保存节流**：Save 改 500ms 防抖或后台队列，消除 UI 线程同步 IO（R7），为提高条数上限铺路。
8. **多显示器/DPI 细化**：停靠逻辑用 `Screen.FromPoint` 计算工作区，替代全局 `SystemParameters.WorkArea`。
9. **安装体验**：install.bat 可选写入 HKCU Run 实现开机自启（用户级，无需管理员）。

---

## 附：源码文件索引

| 文件 | 行数职责摘要 |
|---|---|
| `App.xaml.cs` | 单实例 Mutex、异常兜底、error.log |
| `App.xaml` | StartupUri 指向主窗口 |
| `Models/ClipItem.cs` | ClipItem + ClipType |
| `Native/WinApi.cs` | 全部 P/Invoke 声明 |
| `Services/ClipboardMonitorService.cs` | 隐藏窗口监听 + 重试读取 + 双层自写过滤 |
| `Services/TableParser.cs` | IsTable / Parse / Cell / SplitSingleColumn 开关 |
| `Services/PasteService.cs` | 写剪贴板 + 前台切换 + keybd_event |
| `Services/StorageService.cs` | SHA-256 + 原子 JSON 存取 |
| `Services/TrayService.cs` | Shell_NotifyIcon 托盘 + TaskbarCreated 自愈 |
| `ViewModels/MainViewModel.cs` | 列表状态机 + SyncInPlace + 设置持久化 |
| `Views/MainWindow.xaml(.cs)` | 主窗口交互编排（热键/绑定/粘贴/菜单）+ 窗口位置持久化 |
| `Views/HelpWindow.xaml(.cs)` | 9 步引导（新增空单元格说明） |
| `Views/Converters.cs` | 5 个值转换器（序号性能优化） |
| `Services/SettingsService.cs` | 用户设置持久化（窗口位置/模式/绑定目标） |
| `IsExternalInit.cs` | .NET 3.1 init polyfill |
| `SuperClip.csproj` | 目标框架、发布参数、版本元数据 |
| `build.bat` / `CleanAndBuild.bat` / `PackageRelease.bat` | 构建与打包 |
| `installer/*.bat` | 便携安装/卸载 |
| `README.md` / `CHANGELOG.md` | 用户文档与版本史 |
| `Tests/TableParserTests.cs` | 表格解析逻辑测试（13 个用例） |
| `Tests/StorageServiceTests.cs` | 哈希计算测试（4 个用例） |
| `Tests/MainViewModelTests.cs` | 模型属性测试（4 个用例） |
